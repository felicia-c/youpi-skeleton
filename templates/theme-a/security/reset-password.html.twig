{% extends 'theme-a/base.html.twig' %}

{% block title %}Reset Password !{% endblock %}

{% block body %}
    <section class="page-section">
        <div class="container">
            <div class="row">
                <div class="col-md-4 col-md-offset-4">
                    {{ form_start(form) }}

                    <h1 class="h3 mb-3 font-weight-normal">Réinitialiser mon mot de passe</h1>
                    <div class="form-group">
                        {{ form_label(form.password.first) }}
                        {{ form_widget(form.password.first) }}
                    </div>
                    <div class="form-group">
                            {{ form_label(form.password.second) }}
                            {{ form_widget(form.password.second) }}
                    </div>
                        <!--    <label for="inputPassword" class="sr-only">Nouveau mot de passe</label>
                            <input type="password" name="password" id="inputPassword" class="form-control" placeholder="Nouveau mot de passe" required>
                            -->

                    <!--
                        <div class="form-group">
                            <label for="inputPassword" class="sr-only">Confirmer le mot de passe</label>
                            <input type="password" name="confirm-password" id="inputConfirmPassword" class="form-control" placeholder="Confirmer le mot de passe" required>
                        </div>
                        -->
                    <input type="hidden" name="token" value="{{ token }}">
                    <button class="btn btn-lg btn-primary" type="submit">
                        Enregistrer
                    </button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}